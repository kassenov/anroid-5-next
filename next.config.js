/** @type {import('next').NextConfig} */
const nextConfig = {
    transpilePackages: [
        "react",
        "react-dom",
        "next",
        "bootstrap",
        "react-qrcode-logo",
        "qr-code-styling-new"
    ]
}

module.exports = nextConfig
